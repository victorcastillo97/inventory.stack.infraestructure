apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: msqyl-claim
  labels:
    app: mysql
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi